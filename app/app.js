import {answered,correct,eligible,submit,shuffle} from './engine.js';
const root=document.querySelector('#app'), key='secplus-practice-v1';
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let bank,sections,state={version:1,studied:[],attempt:null,history:[]},view='home',index=0,storageWarning='';
function save(){try{localStorage.setItem(key,JSON.stringify(state));}catch{storageWarning='Browser storage is unavailable or full. Keep this page open and export a backup.';}}
function button(label,action,cls=''){return `<button class="${cls}" data-action="${action}">${label}</button>`;}
function notice(){return storageWarning?`<p role="alert" class="notice">${esc(storageWarning)}</p>`:'';}
function score(a){return a.questions.filter(q=>correct(q,a.answers[q.id])).length;}
function render(){root.innerHTML=notice()+(view==='quiz'?quiz():view==='results'?results():home());root.querySelectorAll('[data-action]').forEach(el=>el.onclick=()=>act(el.dataset.action));root.querySelectorAll('[data-section]').forEach(el=>el.onchange=()=>{state.studied=el.checked?[...new Set([...state.studied,el.dataset.section])]:state.studied.filter(s=>s!==el.dataset.section);save();render();});root.querySelectorAll('[name="answer"]').forEach(el=>el.onchange=()=>{const a=state.attempt,q=a.questions[index];if(a.submitted)return; a.answers[q.id]=[...root.querySelectorAll('[name="answer"]:checked')].map(x=>x.value);save();render();root.querySelector(`[name="answer"][value="${el.value}"]`)?.focus();});root.querySelector('#import')?.addEventListener('change',importFile);root.querySelector('#missed')?.addEventListener('change',e=>{root.querySelectorAll('.review.correct').forEach(x=>x.hidden=e.target.checked);});}
function home(){let count=state.studied.length;return `<section class="hero"><p class="eyebrow">YOUR SECURITY+ STUDY COMPANION</p><h1>Practice with purpose.<br><em>Know where you stand.</em></h1><p>Answer first. Review the reasoning after. Every question leads back to your course material.</p></section><div class="stats"><div><strong>${count}<small> / 26</small></strong><span>Sections studied</span></div><div><strong>${bank.length}</strong><span>Questions in the pilot</span></div><div><strong>${state.history.length}</strong><span>Completed attempts</span></div></div>${state.attempt&&!state.attempt.submitted?`<div class="notice">You have an unfinished exam. ${button('Resume exam','resume','primary')}</div>`:''}<section><div class="section-head"><h2>Choose your practice</h2><span class="tag">PILOT RELEASE</span></div><div class="modes"><article><span class="number">01 / FOCUSED</span><h3>Section practice</h3><p>20 questions on Fundamentals of Security. Objectives 1.1 and 1.2.</p>${button('Start Fundamentals →','start','primary')}<small>Other section banks are being developed.</small></article><article><span class="number">02 / CONNECTED</span><h3>Cumulative practice</h3><p>Available questions from the sections you’ve marked studied.</p>${button('Start cumulative →','cumulative')}<small>${eligible(bank,state.studied).length} available · Only Fundamentals has questions today.</small></article><article class="muted"><span class="number">03 / EXAM DAY</span><h3>Full exam</h3><p>90 questions, 90 minutes, balanced across the five exam domains.</p><button disabled>Not yet available</button><small>Requires all 26 sections and a complete question bank.</small></article></div></section><section class="lower"><div><h2>Your course progress</h2><p class="subtle">Mark a section after studying it. Preparation sections are excluded.</p><div class="sections">${sections.map(s=>`<label><input type="checkbox" data-section="${s.id}" ${state.studied.includes(s.id)?'checked':''}><span><b>${String(s.content_order).padStart(2,'0')}</b> ${esc(s.title)}</span>${s.id==='content-01'?'<small>Quiz ready</small>':''}</label>`).join('')}</div></div><aside><h2>Recent attempts</h2>${state.history.length?state.history.slice().reverse().map((a,i)=>`<button class="history" data-action="history:${state.history.length-1-i}"><span>${new Date(a.submitted).toLocaleDateString()}<small>${esc(a.mode)}</small></span><strong>${score(a)}/${a.questions.length} →</strong></button>`).join(''):'<p class="subtle">Your first result will appear here. Repeated attempts reuse the pilot bank.</p>'}<div class="backup"><h3>Keep your progress</h3><p>Export a backup to move between browsers or protect against cleared browser data.</p>${button('Export progress','export')}<label class="file">Import backup<input id="import" type="file" accept="application/json"></label></div></aside></section>`;}
function quiz(){const a=state.attempt,q=a.questions[index],done=a.questions.filter(q=>answered(q,a.answers[q.id])).length;return `<div class="section-head">${button('← Save & exit','home')}<span class="tag">${esc(a.mode)}</span></div><section class="exam"><div class="section-head"><p class="eyebrow">QUESTION ${index+1} OF ${a.questions.length}</p><span>${done} answered</span></div><progress value="${done}" max="${a.questions.length}" aria-label="Questions answered"></progress><h1 class="question">${esc(q.stem)}</h1><p class="subtle">${q.type==='multiple'?`Select exactly ${q.correct.length} answers.`:'Select one answer.'} Feedback is shown after submission.</p><fieldset><legend class="sr">Answer choices</legend>${q.options.map((o,i)=>`<label class="option ${a.answers[q.id]?.includes(o.id)?'selected':''}"><input name="answer" type="${q.type==='multiple'?'checkbox':'radio'}" value="${o.id}" ${a.answers[q.id]?.includes(o.id)?'checked':''}><span class="letter">${String.fromCharCode(65+i)}</span><span>${esc(o.text)}</span></label>`).join('')}</fieldset><div class="navigation"><button data-action="prev" ${index===0?'disabled':''}>← Previous</button>${button(index===a.questions.length-1?'Review answers':'Next question →',index===a.questions.length-1?'overview':'next','primary')}</div><nav class="question-grid" aria-label="Question navigation">${a.questions.map((q,i)=>`<button aria-label="Question ${i+1}${answered(q,a.answers[q.id])?', answered':', unanswered'}" ${i===index?'aria-current="step"':''} class="${answered(q,a.answers[q.id])?'done':''}" data-action="goto:${i}">${i+1}</button>`).join('')}</nav><p class="subtle">Review any question before submitting. Multi-select questions require all requested choices.</p><button class="primary" data-action="submit" ${done!==a.questions.length?'disabled':''}>Submit all answers</button><p id="message" role="status"></p></section>`;}
function results(){const a=state.attempt,n=score(a);return `${button('← Back to practice','home')}<section class="result-banner"><p class="eyebrow">EXAM COMPLETE</p><h1>${n}<small> / ${a.questions.length}</small></h1><h2>${Math.round(n/a.questions.length*100)}% correct · ${a.questions.length-n} to revisit</h2><p>Practice score, not a prediction of your CompTIA scaled score.</p></section><div class="section-head"><h2>Your answer review</h2><label><input id="missed" type="checkbox"> Only show missed questions</label></div>${a.questions.map((q,i)=>{let ok=correct(q,a.answers[q.id]);return `<article class="review ${ok?'correct':'incorrect'}"><span class="result-label">${ok?'CORRECT':'REVISIT'} · QUESTION ${i+1}</span><h3>${esc(q.stem)}</h3><p><b>Your answer:</b> ${q.options.filter(o=>a.answers[q.id]?.includes(o.id)).map(o=>esc(o.text)).join('; ')}</p><p><b>Correct answer:</b> ${q.options.filter(o=>q.correct.includes(o.id)).map(o=>esc(o.text)).join('; ')}</p><p>${esc(q.explanation)}</p><div class="citation">${esc(q.source.title)} · ${esc(q.source.section)} · PDF page ${q.source.page}<br>Objective ${esc(q.objective)} · Open your local course guide at this page.</div></article>`;}).join('')}`;}
function start(mode){if(state.attempt&&!state.attempt.submitted){view='quiz';render();return;}let pool=mode==='Section practice'?bank:eligible(bank,state.studied);if(!pool.length){alert('Mark Fundamentals of Security as studied to use its questions in cumulative practice.');return;}state.attempt={id:crypto.randomUUID(),mode,started:Date.now(),submitted:null,questions:shuffle(pool).map(q=>({...structuredClone(q),options:shuffle(q.options)})),answers:{}};index=0;view='quiz';save();render();}
function act(action){const [name,value]=action.split(':');if(name==='start')return start('Section practice');if(name==='cumulative')return start('Cumulative practice');if(name==='home')view='home';if(name==='resume')view='quiz';if(name==='prev')index--;if(name==='next')index++;if(name==='goto')index=Number(value);if(name==='overview'){const first=state.attempt.questions.findIndex(q=>!answered(q,state.attempt.answers[q.id]));if(first>=0)index=first;else{root.querySelector('[data-action="submit"]').focus();return;}}if(name==='submit'){try{state.attempt=submit(state.attempt);state.history.push(structuredClone(state.attempt));save();view='results';}catch(e){alert(e.message);return;}}if(name==='history'){if(state.attempt&&!state.attempt.submitted){alert('Finish your current exam before opening an older result.');return;}state.attempt=structuredClone(state.history[Number(value)]);view='results';}if(name==='export'){let u=URL.createObjectURL(new Blob([JSON.stringify(state,null,2)],{type:'application/json'})),link=document.createElement('a');link.href=u;link.download='security-plus-progress.json';link.click();setTimeout(()=>URL.revokeObjectURL(u),1000);return;}render();window.scrollTo({top:0,behavior:'smooth'});}
function valid(s){if(s?.version!==1||!Array.isArray(s.studied)||!s.studied.every(id=>sections.some(x=>x.id===id))||!Array.isArray(s.history)||s.history.length>1000)return false;const check=a=>a&&typeof a.mode==='string'&&Number.isFinite(a.started)&&(!a.submitted||Number.isFinite(a.submitted))&&Array.isArray(a.questions)&&a.questions.length>0&&a.questions.length<=90&&new Set(a.questions.map(q=>q.id)).size===a.questions.length&&a.answers&&typeof a.answers==='object'&&a.questions.every(q=>typeof q.id==='string'&&typeof q.stem==='string'&&typeof q.explanation==='string'&&['single','multiple'].includes(q.type)&&Array.isArray(q.options)&&q.options.length>=2&&q.options.every(o=>typeof o.id==='string'&&/^[a-zA-Z0-9_-]{1,60}$/.test(o.id)&&typeof o.text==='string')&&new Set(q.options.map(o=>o.id)).size===q.options.length&&Array.isArray(q.correct)&&q.correct.length>0&&q.correct.every(id=>q.options.some(o=>o.id===id))&&q.source&&typeof q.source.title==='string'&&typeof q.source.section==='string'&&Number.isInteger(q.source.page)&&(!a.answers[q.id]||Array.isArray(a.answers[q.id])&&a.answers[q.id].every(id=>q.options.some(o=>o.id===id)))&&(!a.submitted||answered(q,a.answers[q.id])));return (!s.attempt||check(s.attempt))&&s.history.every(a=>a.submitted&&check(a));}
async function importFile(e){try{let f=e.target.files[0];if(!f)return;if(f.size>5000000)throw Error('Backup is too large.');let s=JSON.parse(await f.text());if(!valid(s))throw Error('This is not a valid version 1 progress backup.');if(!confirm('Replace progress in this browser with this backup? Export first if you want to keep your current progress.'))return;state=s;index=0;save();render();}catch(err){alert(err.message);}}
try{[bank,sections]=await Promise.all(['questions.json','sections.json'].map(async url=>{const r=await fetch(url);if(!r.ok)throw Error('Could not load practice data.');return r.json();}));try{const raw=localStorage.getItem(key);if(raw){const s=JSON.parse(raw);if(valid(s))state=s;else storageWarning='Saved progress could not be loaded. Import a valid backup to recover it.';}}catch{storageWarning='Browser storage could not be read. Use export to keep your progress.';}render();}catch(e){root.innerHTML=`<h1>Practice could not load</h1><p>${esc(e.message)} Open this app using its hosted link or a local web server, then refresh.</p>`;}
